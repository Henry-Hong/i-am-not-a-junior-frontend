name: on pull request review comment

on:
  pull_request_review:
    types: [submitted]

jobs:
  comment:
    runs-on: ubuntu-latest
    steps:
      - name: checkout-branch
        uses: actions/checkout@v3

      - name: check-comment
        id: check-comment
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const {data: reviewComments} = await github.rest.pulls.listReviewComments({ 
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
            }) ?? [];

            const recentComment = reviewComments.at(-1);
            const reviewId = recentComment.pull_request_review_id;
            const reviewCommentsInThisReview = reviewComments.filter(comment => comment.pull_request_review_id === reviewId);
            const isReviewCommentsContainNIT = reviewCommentsInThisReview.some(comment => comment.body.includes('NTC'));

            if(isReviewCommentsContainNIT) return true;
            else return false;

      - name: make-this-pr-to-draft
        if: ${{ steps.check-comment.outputs.result == 'true' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh pr ready --undo ${{ github.event.pull_request.number }}
